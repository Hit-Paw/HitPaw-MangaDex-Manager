# src/api — MangaDex API

Future home for `MainWindow::fetch*`, token refresh, pagination (`Show more (60)` / `Show All (N)`), cover CDN retry/backoff. Keep `secure_store.h` as QSettings → OS keychain facade.
